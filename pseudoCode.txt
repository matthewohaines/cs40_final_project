    Car {
        Section *one;
        Section *two;
        
        Car() {
            one = nullptr;
            two = nullptr;
        }
        }
    
    if(probalites...) {
        Car newCar;
        
    }
    void placeCar() {
        if(firstSection->forward.isOccupied() == true) {
        newCar->one = northBound.firstSection;
        newCar->two = northBound.firstSection->forward;
        
        }
        newCar->one = northBound.firstSection;
        
    }
    
    void move() {
        
        if(newCar->two == nullptr) {
        }
    }
    }

Quueue cars {1, 2, 3, 4, 5}
for car:cars {
    